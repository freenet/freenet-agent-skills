{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'command=\"$(echo \"$STDIN\" | jq -r \".tool_input.command // empty\")\"; if [[ \"$command\" == *\"git commit\"* && -f Cargo.toml ]]; then echo \"🔍 Running pre-commit checks...\"; if ! cargo fmt --check 2>&1; then echo \"❌ Formatting check failed. Run: cargo fmt\"; exit 1; fi; if ! cargo clippy --all-targets --all-features -- -D warnings 2>&1; then echo \"❌ Clippy check failed. Fix warnings above.\"; exit 1; fi; echo \"✅ Pre-commit checks passed\"; fi'"
          }
        ]
      }
    ]
  }
}
